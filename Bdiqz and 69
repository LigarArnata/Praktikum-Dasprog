// Bdiqz and 69

#include <stdio.h>
#include <math.h> 
int main(){

     long n, x, hasil;
    double pembilang;

    scanf("%ld", &n);
    x = n + 1;

    pembilang = (1+sqrt(5))/2;
    hasil = 2*round(pow(pembilang,x)/sqrt(5));

    printf("%ld\nBdiqz tidak ganz!", hasil);
      
}
